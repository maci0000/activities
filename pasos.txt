1ro 
crear una carpeta en algun lugar(Downloads, Desktop, etc)
2do en el git:
dir
cd Downloads
cd ~/Downloads/clase_git
git init(INICIALIZAR)
git status(VER EL ESTADO)
~ aun en el working directory

$ git add . (staging area)
 o $ git add util/pasos.txt
git commit -m ' '(agrega el comentario para empezar la trazabilidad)
git log (muestra el historial de coomits)

~siempre que se haga un cambio se debe hacer todo lo anterior

--RAMAS--
$ git branch rama1 (crea una rama)
$ git branch (visualizar las ramas y la que esta activa con *)
$ git branch rama1 (activar rama)
git checkout master(cambia auna rama o a master)
git merge rama1 (añade los cambios de la rama a master)
git branch -d rama1 (borra la rama)

#al pasar la rama al master luego se debe eliminar la rama

git log --oneline(muestra los commit de forma resumida)
git log --oneline --graph (grafica las ramas y los merges
)
